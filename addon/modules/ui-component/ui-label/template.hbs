{{yield }}

<div class="effect-container">

  {{#if emoji}}
    <div class="emoji-wrapper">
      <span class={{emojiClass}}>{{emoji}}</span>
    </div>
  {{else}}

    <div class="svg-wrapper">
      {{#if icon}}
        {{svg-item icon size=size viewBox=viewBox}}
      {{else}}
        {{svg}}
      {{/if}}
    </div>
  {{/if}}

  {{#if selected}}
    {{particle size=particleSize}}
  {{/if}}

</div>

{{#if text}}
  <pre>{{{insert-img text}}}</pre>
{{/if}}
