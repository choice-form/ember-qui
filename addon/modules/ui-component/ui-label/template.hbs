{{yield }}

<div class="effect-container">

  {{#if emoji}}
    <div class="emoji-wrapper">
      <span class={{emojiClass}}>{{emoji}}</span>
    </div>
  {{else}}

    <div class="svg-wrapper">
      {{#if icon}}
        {{component "ui-addon/svg-item" icon=icon size=size viewBox=viewBox iconClass=iconClass}}
      {{else}}
        {{svg}}
      {{/if}}
    </div>
  {{/if}}

  {{#if selected}}
    {{ui-addon/svg-particle}}
  {{/if}}

</div>

{{#if text}}
  <span>{{text}}</span>
{{/if}}
