<input
  class={{className}}
    type={{type}}
  value={{value}}
    required
  placeholder={{placeholder}}
    oninput={{handleOptionInput}}
  onchange={{handleOptionInput}}
>
{{svg-item icon size=size viewbox=viewbox}}

{{errorMessage}}
