<div class="weight-header">
  <h4>权重指示</h4>
  <div class="weight-chart">
    {{#each options as |option|}}
      {{html-safe (concat "<div style='flex-basis:" option.value "%;" "background:" option.color "'></div>")}}
    {{/each}}
  </div>
</div>

{{#each options as |option|}}
  <div class="ui-range">
    {{#if option.text}}
      <h4 class="range-title">{{option.text}}</h4>
    {{/if}}

    <div class="range-slider">
      <h4>{{if option.value option.value ' - '}}{{node.unit}}</h4>
      {{range-slider
        start=(if option.value (numberize displayValue) 0)
        connect="lower"
        min=0
        tooltips=tipValue
        max=(numberize option.maxValue)
        on-change=(action "handleOptionInput" option)
      }}
    </div>
  </div>
{{/each}}