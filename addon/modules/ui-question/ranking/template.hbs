{{#each node.options as |option|}}
  {{#if (eq option.inputType "input")}}
    {{ui-question/-node/text-cell node option handleEvents}}
  {{else}}
    {{ui-question/ranking/ranking-item option sortNo=option.sortNo}}
  {{/if}}
{{/each}}