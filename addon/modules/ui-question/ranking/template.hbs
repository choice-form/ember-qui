{{#each node.options as |option|}}
  <div class={{concat 'sort-list' (if option.sortNo ' sort')}}>
    <span>{{option.sortNo}}</span>
    {{ui-addon/label
      uuid=option.uuid
      text=option.text
      rightIcon=option.icon
    }}
  </div>
{{/each}}