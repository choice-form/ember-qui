<input
  id={{option.uuid}}
    type={{node.selectType}}
  onclick={{action 'handleOptionClick'}}
    name={{node.uuid}}
  autofocus={{autofocus}}
    placeholder={{option.placeholder}}
  checked={{option.selected}}
    disabled={{disabled}}
  required={{required}}
    tabindex={{tabindex}}
  value={{option.value}}
>

<label for={{option.uuid}}>
  {{#if node.thumbnail}}
    <div class="thumbnail">
      {{image}}
    </div>
  {{else}}
    {{image}}
  {{/if}}
  <div class="pic-description">
    <div class="select-container">
      <div class="svg-wrapper">
        {{svg}}
      </div>
      <div class="svg-particle">
        {{ui-component/ui-icon icon="svg-particle" size="60px" viewBox="60"}}
      </div>
    </div>
    <span>{{option.text}}</span>
  </div>
</label>