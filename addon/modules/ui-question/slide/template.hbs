{{ui-component/ui-slide justifyContent=(unbound node.justifyContent)
                        backgroundImage=(unbound node.backgroundImage)
                        backgroundPosition=(unbound node.backgroundPosition)
                        backgroundRepeat=(unbound node.backgroundRepeat)
                        backgroundSize=(unbound node.backgroundSize)
                        options=(unbound node.options)
}}
