{{ui-component/ui-verification renderId=(unbound node.renderId)
                               icon=(unbound node.verificationType)
                               verificationType=(unbound node.verificationType)
                               captchaClass=captchaClass
                               buttonText=buttonText
                               countDown=countDown
                               getInfoButton=getInfoButton
                               captcha=node.captcha
                               declarative=(unbound node.declarative)
                               handleOptionClick=(action "handleOptionClick")
                               handleOptionInput=(action "handleOptionInput")
}}
