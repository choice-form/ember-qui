{{!--如果输入类型是input，则，使用其他选项备注组件--}}
{{#unless (eq option.inputType 'input')}}
  <div class={{concat 'ui-' node.selectType}}>
    <input
      data-color="color8"
      id={{option.uuid}}
        type={{node.selectType}}
      onclick={{action 'handleOptionClick'}}
        name={{node.uuid}}
      autofocus={{autofocus}}
        placeholder={{option.placeholder}}
      checked={{option.selected}}
        disabled={{disabled}}
      required={{required}}
        tabindex={{tabindex}}
      value={{option.value}}
    >
    {{ui-question/-node/label option}}
  </div>
{{else}}
  {{ui-question/-node/text-cell node option handleEvents}}
{{/unless}}


{{!--选择选项再弹出输入框--}}
{{#if (and (eq option.inputType "select-input") option.selected)}}
  {{ui-question/-node/text-cell node option handleEvents}}
{{/if}}

{{errorMessage}}