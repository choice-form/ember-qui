{{#if isTextArea}}

  <textarea
    value={{option.value}}
    placeholder={{option.placeholder}}
    oninput={{action 'handleOptionInputForTextarea'}}
  ></textarea>

{{else}}

  {{ui-question/-node/input-cell
    className=mobiClassName
    type=type
  value=option.value
  placeholder=option.placeholder
    handleOptionInput=(action 'handleOptionInput')
    svgName=svgName
  }}
{{/if}}
{{errorMessage}}