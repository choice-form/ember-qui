{{#if (or emoji svg)}}
  <div class="select-container">
    {{#if emoji}}
      <div class="emoji-wrapper">
        <span class={{emojiClass}}>{{emoji}}</span>
      </div>
    {{else}}
      <div class="svg-wrapper">
        {{svg}}
      </div>
    {{/if}}
    {{ui-addon/active-line-animation circle=true}}
  </div>
{{/if}}


{{component leftIcon icon=icon size=size viewBox=viewBox iconClass=iconClass}}

{{yield}}

{{#if text}}
<span>{{text}}</span>
{{/if}}

{{component rightIcon icon=icon size=size viewBox=viewBox iconClass=iconClass}}

