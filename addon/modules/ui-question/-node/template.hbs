{{ui-question/-node/header headerData}}
<div class={{concat "control " node.quesType " " node.showStyle " " node.picStyle}}>
  {{#if isSpecialComponent}}
    <div class='cell'>
      {{component componentName node handleEvents}}
    </div>
  {{else}}
    {{#each node.options as |option|}}
      <div class='cell'>
        {{component componentName node option handleEvents}}
      </div>
    {{/each}}
  {{/if}}
</div>

{{#if isLoading}}
  <div>
    loading............
  </div>
{{/if}}


{{!--{{component 'ui-question/-node/option-cell' node option handleEvents}}--}}
